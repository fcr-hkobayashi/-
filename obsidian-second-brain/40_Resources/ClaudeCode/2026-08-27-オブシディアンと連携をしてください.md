---
title: "オブシディアンと連携をしてください"
created: 2026-08-27
updated: 2026-08-27
session_id: 6418bf03-4cb0-4afe-9cf7-33feab284cd4
tags: [claude-code, resource]
type: resource
status: active
source: claude-code
cwd: "/Users/hayato.kobayashi/Downloads/みらいやAI"
git_branch: HEAD
---

# オブシディアンと連携をしてください

> Claude Code セッション記録（2026-08-27 〜 2026-08-27）。ツール実行ログは除外。

### **あなた**

オブシディアンと連携をしてください。

### **Claude**

少し状況を確認しました。

- 作業ディレクトリ: `みらいやAI/attendance-app`（Flask + SQLite の勤怠アプリ）
- Obsidian Vault: `~/second-brain-repo/obsidian-second-brain`（PARA構成、CLAUDE.md・スラッシュコマンド・インポートツール済みの本格的なセカンドブレイン）

「連携」の中身で作業が大きく変わるので、1つだけ確認させてください。

### **Claude**

まず既存のObsidian設定を全部読み込みます。

### **Claude**

全体像を把握しました。この作業ディレクトリからVaultを同じように操作できるようにします。まずVaultへのアクセス権を追加します。

### **Claude**

Vaultにアクセスできるようになりました。次に、この作業ディレクトリから同じ操作ができるようにブリッジを作ります。

### **Claude**

Obsidianとの連携が完了しました。既存のセカンドブレインの内容を全て把握した上で、この作業ディレクトリから**Vault内で起動したときと同じことができる**状態にしてあります。

## 把握した既存の仕組み

`~/second-brain-repo/obsidian-second-brain`（GitHubリポジトリ連動、Obsidianで現在開いているVault）

- **構成**: PARA + Zettelkasten（`00_Inbox` → `10_Daily` → `15_Meetings` → `20_Projects`/`30_Areas`/`40_Resources` → `50_Archive`、`60_Permanent`が永久ノート）
- **運用ルール**: `CLAUDE.md` に frontmatter規約・命名規則・`[[ウィキリンク]]`方針・編集ルール（本文を勝手に消さない、`created`は保持し`updated`だけ更新、破壊的操作は事前確認）
- **既存コマンド7つ**: `/daily` `/inbox-zero` `/permanent` `/weekly-review` `/meet-sync` `/claude-import` `/claude-code-import`
- **取り込みツール**: `tools/claude_chat_import.py`（claude.aiのエクスポート）、`tools/claude_code_import.py`（Claude CodeのローカルJSONL）— どちらもuuid/session_idで重複スキップ

## 今回やったこと

1. **Vaultへのアクセス権を追加** — このセッションから直接読み書きできることを確認済み（下記のノート作成で実証）
2. **[CLAUDE.md](CLAUDE.md) を新規作成** — Vaultの絶対パス、フォルダ構成、守るべき編集ルールを記載。Vault操作時はまず本家 `$VAULT/CLAUDE.md` を読んでそれに従う入口として機能します
3. **[.claude/commands/](.claude/commands) にコマンド8つを移植** — 既存7つを、相対パスを全てVaultの絶対パスに書き換えて配置。加えて `/vault`（Vaultの現状確認、読み取り専用）を新規追加
4. **[20_Projects/みらいやAI.md](../../second-brain-repo/obsidian-second-brain/20_Projects/みらいやAI.md) をVaultに作成** — この案件のプロジェクトノート。テンプレート準拠、意思決定ログに今回の連携を記録、既存の `[[2026-08-21-Obsidian×ClaudeCodeセットアップ]]` へリンク

## 現在のVaultの状態

| フォルダ | ノート数 |
|---|---|
| 00_Inbox | 1（滞留メモ） |
| 10_Daily | 1 |
| 15_Meetings | 2 |
| 20_Projects | 1（今回作成） |
| 30_Areas | 1 |
| 40_Resources | 25（ClaudeChats 19 / ClaudeCode 5） |
| 60_Permanent | 1 |

未完了タスク `- [ ]` は全体で8件です。

**1点だけ注意**: 新しく置いたスラッシュコマンドは、セッション起動時に読み込まれる仕様のため、この会話ではまだ `/daily` などが認識されない可能性があります。認識されない場合はセッションを開き直してください。認識されなくても「今日のデイリーノート作って」「Inboxを整理して」と自然文で頼めば同じ処理を実行できます。

まず動作確認として `/daily` で本日（2026-08-27）のデイリーノートを作りますか？ それとも滞留している `00_Inbox` の整理から始めますか？

### **あなた**

両方行ってください

### **Claude**

両方進めます。まず本日分のデイリーノートを作成します。

### **Claude**

デイリーノートを作成しました（引き継ぎタスク3件を集約）。

続いて `/inbox-zero` です。`00_Inbox/` を確認したところ、実体のあるメモは1件でした。Vaultのルールに「移動前に一覧を提示して確認を取ること」とあるので、先に整理案を出します。

**整理案**

| ファイル | 判定 | 移動先 | 付随する編集 |
|---|---|---|---|
| `2026-08-21-投資メモ.md` | 知見は既に永久ノート化済みの**出典メモ**（資料） | `40_Resources/` | `type: inbox → resource`、`tags` から `未分類` を削除、`updated` を今日に更新、末尾の「サンプル」注記を実際の整理結果に置き換え |
| `_about.md` | フォルダ説明（meta） | 移動しない | なし |

補足：このメモ内の `#要ToDo`「NISAの枠を調べる」は既に [[家計]] と本日のデイリーに集約済みなので、新規タスク作成は不要です。破棄すべきメモはありません。

### **Claude**

両方完了しました。

## 1. `/daily` — 本日のデイリーノート

[10_Daily/2026-08-27.md](../../second-brain-repo/obsidian-second-brain/10_Daily/2026-08-27.md) を新規作成しました（既存ファイルは無かったので上書きなし）。

- テンプレート `90_Templates/デイリーノート.md` 準拠、`{{date}}`/`{{yesterday}}`/`{{tomorrow}}` を 2026-08-27 / 08-26 / 08-28 に置換
- **引き継ぎタスク3件を集約**
  - NISAの枠を調べる（[[家計]] より・2026-08-21から持ち越し）
  - attendance-app の残課題を洗い出す（[[みらいやAI]] より）
  - 開発中の決定事項を意思決定ログに残す運用（[[みらいやAI]] より）
- 会議ノート内の `- [ ]（担当 / 期限）` はテンプレートの空プレースホルダーなので除外しました

## 2. `/inbox-zero` — Inbox整理

**移動**: `00_Inbox/2026-08-21-投資メモ.md` → [40_Resources/2026-08-21-投資メモ.md](../../second-brain-repo/obsidian-second-brain/40_Resources/2026-08-21-投資メモ.md)

- frontmatter: `type: inbox → resource`、`tags` から `未分類` を削除して `[お金, 投資]` に、`updated` を 2026-08-27 に更新（`created` は 2026-08-21 のまま保持）
- 「🔗 関連」セクションを追加し、[[家計]] と [[複利は時間を味方につける]] へ明示的にリンク
- 末尾の「サンプル」注記を、実際の整理結果の記録に置き換え
- 本文は削除せずそのまま保持

**リンク健全性**: ファイル名を変えていないため、`60_Permanent/複利は時間を味方につける.md` と `30_Areas/家計.md` からの `[[2026-08-21-投資メモ]]` は移動後も有効です。

`00_Inbox/` は `_about.md` のみになり、**Inbox Zero 達成**です。永久ノート化すべき新規の知見はありませんでした（既に `複利は時間を味方につける` として昇華済み）。

## Git の状態（未コミット）

Vaultは現在 `claude/obsidian-claude-code-integration-hukuuu` ブランチ（`main` ではありません）にいて、以下が未コミットです。

```
?? 10_Daily/2026-08-27.md
?? 20_Projects/みらいやAI.md
?? 40_Resources/2026-08-21-投資メモ.md
 D 00_Inbox/2026-08-21-投資メモ.md
 M .obsidian/app.json（Obsidianアプリ側の設定変更・今回の作業とは無関係）
```

コミット・プッシュしますか？ その場合、ノート3件の変更のみを対象にし、`.obsidian/` の設定変更は含めない形にできます。ブランチが `main` ではないので、`main` に取り込みたい場合はPR経由になる点もあわせてご確認ください。

### **あなた**

Gitも連携して欲しいです

### **Claude**

Git連携を進めます。まず現状を正確に確認します。

### **Claude**

状況が分かりました。重要な点が1つあります。

**Vaultの中身は全て `claude/obsidian-claude-code-integration-hukuuu` ブランチにしか存在せず、`main` は9コミット分遅れています**（`main` にあってこちらに無いコミットは0件なので、`main` への統合はfast-forwardで安全にできます）。また `gh` コマンドは未インストールのため、PR作成はCLIからはできません。

コミット対象の方針を確認させてください。

### **Claude**

質問を閉じられたので、コミット・プッシュは実行せずに止めています。Vaultの変更はローカルに未コミットのまま、そのまま残っています。

現状の整理だけ置いておきます。

**未コミットの変更（Vault内）**
- 新規: `10_Daily/2026-08-27.md`、`20_Projects/みらいやAI.md`、`40_Resources/2026-08-21-投資メモ.md`
- 削除: `00_Inbox/2026-08-21-投資メモ.md`（上記への移動によるもの）
- 変更: `.obsidian/app.json` ほか設定ファイル（今回の作業とは無関係のObsidianアプリ側の変更）

**判明した前提**
- 現在のブランチは `claude/obsidian-claude-code-integration-hukuuu`。Vaultの中身9コミット分は**このブランチにしか無く**、`main` は遅れています（統合はfast-forward可能）
- `gh` コマンドが未インストールのため、CLIからのPR作成はできません

進め方が決まりましたら、「現ブランチにコミットして」「mainに統合して」などと指示してください。その内容でコミット・プッシュします。

また、今後ノートを更新するたびに自動でコミット・プッシュする `/vault-sync` のようなコマンドを `.claude/commands/` に追加することもできます。必要であればお申し付けください。

### **あなた**

https://couples-navi.com/2026/05/09/claude-code-obsidian-second-brain-setup/

この記事を読んでオブシディアンとclaudecodeを連動させてください。

### **Claude**

記事を読みます。

### **Claude**

記事を読みました。既存Vaultとの差分を確認するため、Obsidian側の現状を調べます。

### **Claude**

記事の内容と既存Vaultの差分が明確になりました。実装します。

**方針**: 記事のフォルダ構成（`01_Projects` 等）に既存Vaultをリネームすると、既に書かれたノートの `[[リンク]]` が壊れるだけで得がないため、**フォルダ構成は既存のまま維持**し、記事で未実施だった部分（Git設定・`06_Claude` 相当・デイリーノート設定・プラグイン）を足します。

### **Claude**

Obsidianの「今日のデイリーノート」が `10_Daily/` にテンプレート適用で作られるようになりました。次に記事の `06_Claude/` に相当する領域を、既存の番号体系に合わせて作ります。

### **Claude**

Vault側の整備が済みました。次に、記事のGit運用を実行するための `/vault-sync` コマンドを両側に作ります。

### **Claude**

記事の内容をVaultに反映しました。

## 記事と既存Vaultの照合

記事は「Vault = 1つのGitリポジトリ」「`00_Inbox`〜`06_Claude` + `Daily`」という構成でしたが、既にノートが書かれ `[[リンク]]` が張られているため、**フォルダのリネームは行いませんでした**（リンクが壊れるだけで得がないため）。記事の思想だけを取り込んでいます。対応表は下記の記録ノートに残しました。

## 実施したこと

**1. Git設定（記事準拠）** — [.gitignore](../../second-brain-repo/obsidian-second-brain/.gitignore) を記事の内容に強化
- `.obsidian/workspace.json`（PC固有・毎回差分が出る）、`.obsidian/plugins/*/data.json`、`.trash/`、`.claude/settings.local.json`、`*.tmp` `*.log`、`.smart-env`、`Thumbs.db` を除外
- リポジトリ直下にも `.DS_Store` 用の `.gitignore` を新規作成

**2. `70_Claude/` を作成**（記事の `06_Claude/` 相当を既存の番号体系に合わせて）
- [CONTEXT.md](../../second-brain-repo/obsidian-second-brain/70_Claude/CONTEXT.md) — 「私は誰で、いま何をしているか」。`CLAUDE.md` が*どう書くか*、CONTEXT.md が*誰のために書くか*を担当します。内容は仮置きなので自由に書き換えてください
- `memory/`（作業の途中経過）、`outputs/`（生成物の一時置き場）

**3. Obsidian本体の設定** — Daily notes コアプラグインを設定（フォルダ `10_Daily`、書式 `YYYY-MM-DD`、テンプレート `90_Templates/デイリーノート.md`）。`Cmd+P` →「Open today's daily note」でテンプレート適用済みのノートが正しい場所に作られます

**4. ルールの更新**
- Vaultの [CLAUDE.md](../../second-brain-repo/obsidian-second-brain/CLAUDE.md) に「9. Git運用」を追加、`70_Claude/CONTEXT.md` を読むルールも追加
- [README.md](../../second-brain-repo/obsidian-second-brain/README.md) の「記事にアクセスできなかった」という注記を、実際に読んだ結果に差し替え
- `/vault-sync` コマンドを両側に追加（Git操作用）

**5. 記録ノート** — [40_Resources/2026-08-27-Obsidian×ClaudeCode連携セットアップ.md](../../second-brain-repo/obsidian-second-brain/40_Resources/2026-08-27-Obsidian×ClaudeCode連携セットアップ.md) に、記事との対応表・済/未の一覧・記事の推奨設定値を保存

## ⚠️ 見つかった重要な問題（記事の前提が成り立たない）

記事は **Obsidian Git で30分ごとに自動コミット・プッシュ**を推奨していますが、**このVaultはGitリポジトリのルートではありません**。リポジトリは1つ上の `~/second-brain-repo` で、`ai-secretary-app` `used-market-app` など**開発中の他アプリと同居**しています。

このまま Obsidian Git の自動同期を有効にすると、**書きかけの他プロジェクトのコードごと自動コミット・自動プッシュされます**。

対処案は3つ、記録ノートにも書いてあります。
- **A**: Vaultを独立リポジトリに切り出す（記事の前提に合わせる。自動同期を安全に使える）
- **B**: Obsidian Gitを入れて自動同期はオフ、手動バックアップのみ
- **C**: Obsidian Gitを入れず `/vault-sync` でパス限定コミット（**現在この方式**。`git add -A` を禁止するルールをCLAUDE.mdに明記済み）

## やっていないこと

- **コミュニティプラグインの導入**（Obsidian Git / Templater / Dataview / Tasks / Smart Connections / BRAT+Claudian）。現在**1つも入っていません**（`.obsidian/plugins/` が存在しない）。第三者のコードをダウンロードする操作なので、Obsidianの画面から入れていただくのが安全です。手順と優先順位は記録ノートに書きました
- **コミット・プッシュ**。前回ブランチの選択を保留されたため、変更はすべて未コミットのまま残しています

未コミットの変更は以下です（Vault外は私が作った直下の `.gitignore` のみ、他プロジェクトは無変更を確認済み）。

```bash
git -C ~/second-brain-repo status --short -- obsidian-second-brain/
```

コミットして良ければ「コミットして」、GitHubにも上げるなら反映先のブランチ（現在の `claude/obsidian-claude-code-integration-hukuuu` か、fast-forward可能な `main`）をお知らせください。

### **あなた**

このファイルってどこにありますか？

### **Claude**

開いているのは `00_Inbox/_about` で、実体はここにあります。

```
/Users/hayato.kobayashi/second-brain-repo/obsidian-second-brain/00_Inbox/_about.md
```

Finderで開く場合はこちら。

```bash
open -R "/Users/hayato.kobayashi/second-brain-repo/obsidian-second-brain/00_Inbox/_about.md"
```

**パスの構造**

| 階層 | パス | 中身 |
|---|---|---|
| Gitリポジトリのルート | `~/second-brain-repo/` | Vault + 他アプリ4つ |
| Vault本体 | `~/second-brain-repo/obsidian-second-brain/` | Obsidianが開いているフォルダ |
| このファイル | `.../00_Inbox/_about.md` | Inboxフォルダの説明ノート |

Obsidian上では拡張子 `.md` が隠れるので `_about` と表示されますが、ファイル名は `_about.md` です。

**Obsidian内から場所を確認する方法**: 左サイドバーのファイル名を右クリック →「システムエクスプローラーで表示」（Reveal in Finder）でFinderが開きます。

なお、スクリーンショットの左サイドバーに `70_Claude` が表示されているので、先ほど作成したフォルダがObsidian側にも反映されているのが確認できます。`_about.md` は各フォルダに置いてある「このフォルダの使い方」を書いたメモで、`00_Inbox` `10_Daily` `20_Projects` などにそれぞれ存在します。

### **あなた**

オブシディアンのファイルはどれになりますか？

### **Claude**

現在いる `~/second-brain-repo` の中で、**Obsidianのファイルは `obsidian-second-brain/` フォルダ1つだけ**です。

```
~/second-brain-repo/              ← 今ここ（Gitリポジトリのルート）
├── obsidian-second-brain/        ★ これがObsidian Vault（メモの本体）
├── ai-secretary-app/                Webアプリ（Obsidianとは無関係）
├── ai-vocab-zukan/                  Webアプリ（同上）
├── japan-history-map-app/           Webアプリ（同上）
├── used-market-app/                 Webアプリ（同上）
├── PRODUCT.md
└── .gitignore
```

Obsidianで開いているのは `obsidian-second-brain/` で、スクリーンショット左下に表示されていた `obsidian-second-brain` がこれにあたります。この中に `00_Inbox` `10_Daily` `70_Claude` などのフォルダとノート（`.md`ファイル）が入っています。

**中身の内訳**

| 種類 | パス | 説明 |
|---|---|---|
| ノート | `obsidian-second-brain/00_Inbox/` 〜 `90_Templates/` | Obsidianに表示される `.md` ファイル |
| Obsidianの設定 | `obsidian-second-brain/.obsidian/` | テーマ・プラグイン・Daily notes設定など（隠しフォルダ） |
| Claude Codeの設定 | `obsidian-second-brain/CLAUDE.md`、`.claude/commands/` | 運用ルールとスラッシュコマンド |
| 取り込みツール | `obsidian-second-brain/tools/` | Pythonスクリプト2本 |

**注意点**: この作業ディレクトリ（`~/second-brain-repo`）はVaultより1階層上なので、ここには**Obsidianと無関係な開発中のアプリ4つが同居**しています。前回お伝えした通り、Gitでコミットする際に `git add -A` を使うとそれらを巻き込むため、`git add obsidian-second-brain/` のようにパスを限定する必要があります。

Obsidianのメモだけを扱いたい場合は、Vaultに直接入るのが安全です。

```bash
cd ~/second-brain-repo/obsidian-second-brain
```

### **あなた**

Vaultのファイルにしてください

### **Claude**

作業ディレクトリをVaultに切り替えます。

### **Claude**

作業ディレクトリをVaultに移しました。

```
/Users/hayato.kobayashi/second-brain-repo/obsidian-second-brain
```

これで、記事にあった**方式A（Vault内でClaude Codeを起動する、最もシンプルな形）**と同じ状態になりました。

**この切り替えで変わること**
- `CLAUDE.md`（運用ルール）がセッション開始時に自動で読み込まれる
- `.claude/commands/` のスラッシュコマンドが、絶対パス変換なしのオリジナル版で動く（`/daily` `/inbox-zero` `/permanent` `/weekly-review` `/meet-sync` `/claude-import` `/claude-code-import` `/vault` `/vault-sync`）
- `tools/` のPythonスクリプトを `python3 tools/claude_code_import.py` のように相対パスで実行できる
- 開発中のアプリ4つ（`ai-secretary-app` など）が視界に入らなくなり、誤って触るリスクが減る

現在Vaultには**ノート54件**、フォルダは `00_Inbox` `10_Daily` `15_Meetings` `20_Projects` `30_Areas` `40_Resources` `50_Archive` `60_Permanent` `70_Claude` `90_Templates` があります。

**注意**: Gitリポジトリのルートは依然として1つ上の `~/second-brain-repo` です。作業ディレクトリが変わってもGitの構造は変わらないため、コミット時にパスを限定するルールはそのまま有効です（`/vault-sync` がこれを自動で行います）。

なお、前回からの変更（デイリーノート、プロジェクトノート、Inbox整理、記事反映分）は**まだ未コミット**です。コミットしてよければお知らせください。
