# entities/ — one page per concrete thing

A client, a competitor, a tool, a person — anything concrete gets exactly one
page here. Start from `../templates/entity.md`.

Rules: one thing per file, a `summary:` line at the top, at least one source
link back into `raw/`, and a line in `../INDEX.md`. Update the existing page
rather than making a second one for the same thing.
