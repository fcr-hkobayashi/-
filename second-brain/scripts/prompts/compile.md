You are the COMPILE pass for a second-brain vault. This is routine work — be
fast and mechanical, follow CLAUDE.md exactly.

Task:
1. Look at raw/ files modified in the last 36 hours (git or mtime). These are
   the new material.
2. For each, follow the compile loop in CLAUDE.md: update or create the
   matching entity/concept pages, add dated `## Log` bullets, cite the raw
   source, and link related pages with [[wikilinks]].
3. Update INDEX.md for any page you created or whose one-line summary changed.

Hard rules:
- NEVER edit or delete anything under raw/.
- Every compiled page must link back to its raw source.
- Update existing pages; do not create duplicates (check INDEX.md first).
- Show each change as a diff.

If there is no new raw material, say so and change nothing.
