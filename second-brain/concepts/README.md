# concepts/ — one page per idea

A strategy, a pattern, a lesson — one idea per page. Start from
`../templates/concept.md`.

Rules: one lesson per file, a `summary:` line at the top, at least one source
link back into `raw/`, and a line in `../INDEX.md`. If you catch yourself
writing two lessons on one page, split it.
